# local configuration, per machine
neural-lens.ini

__pycache__/
*.pyc

# never commit the neural stack itself (NVIDIA DLL, ReShade, add-ons)
*.dll
*.addon64
*.addon32

# PyInstaller and Inno Setup output
build/

# what the stack setup and the lens write beside the script when run from here
stack/
ReShade/
downloads/
data/
